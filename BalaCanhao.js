class BalaCanhao {
    constructor(x, y) {
        var parado = {
            isStatic: true
        }
        this.r = 30;

       
    }

    display() {
        var pos = this.body.position;
        push();
        imageMode(CENTER);
         pop();

    }

}